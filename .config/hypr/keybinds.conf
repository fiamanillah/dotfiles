#########################################################################################
# KEYBINDINGS CONFIGURATION
# Engine: hy3 (Manual Tiling & Tabbing)
# Modifier: SUPER (Windows Key)
#########################################################################################

$mainMod = SUPER

# ==========================================
# 1. CORE & APPLICATIONS
# ==========================================
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, R, exec, $menu
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, B, exec, google-chrome-stable --enable-features=UseOzonePlatform --ozone-platform=wayland
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, V, exec, sh -c 'cliphist list | rofi -dmenu -p "󰅌 Clipboard" | cliphist decode | wl-copy'

# ==========================================
# 2. HY3: WINDOW MANAGEMENT (FOCUS & MOVE)
# ==========================================
bind = $mainMod, F, fullscreen,
bind = $mainMod SHIFT, F, togglefloating,

# Move focus (Super + Arrows)
bind = $mainMod, left, hy3:movefocus, l
bind = $mainMod, right, hy3:movefocus, r
bind = $mainMod, up, hy3:movefocus, u
bind = $mainMod, down, hy3:movefocus, d

# Move active window (Super + Shift + Arrows)
bind = $mainMod SHIFT, left, hy3:movewindow, l
bind = $mainMod SHIFT, right, hy3:movewindow, r
bind = $mainMod SHIFT, up, hy3:movewindow, u
bind = $mainMod SHIFT, down, hy3:movewindow, d

# Resize active window (Super + Alt + Arrows)
binde = $mainMod ALT, right, resizeactive, 20 0
binde = $mainMod ALT, left, resizeactive, -20 0
binde = $mainMod ALT, up, resizeactive, 0 -20
binde = $mainMod ALT, down, resizeactive, 0 20

# Move/resize windows with mouse dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# ==========================================
# 3. HY3: SPLITTING & TABBING (MANUAL TILING)
# ==========================================
bind = $mainMod, H, hy3:makegroup, h             # Split next window horizontally
bind = $mainMod, J, hy3:makegroup, v             # Split next window vertically
bind = $mainMod, G, hy3:changegroup, toggletab   # Toggle tabbing mode for current group

# Group focus traversal (For navigating nested splits)
bind = $mainMod, P, hy3:changefocus, raise       # Select parent group
bind = $mainMod SHIFT, P, hy3:changefocus, lower # Select child window

# ==========================================
# 4. WORKSPACES
# ==========================================
# Switch workspaces
bind = $mainMod, 1, split-workspace, 1
bind = $mainMod, 2, split-workspace, 2
bind = $mainMod, 3, split-workspace, 3
bind = $mainMod, 4, split-workspace, 4
bind = $mainMod, 5, split-workspace, 5
bind = $mainMod, 6, split-workspace, 6
bind = $mainMod, 7, split-workspace, 7
bind = $mainMod, 8, split-workspace, 8
bind = $mainMod, 9, split-workspace, 9
bind = $mainMod, 0, split-workspace, 10

# Move active window to a workspace
bind = $mainMod SHIFT, 1, split-movetoworkspace, 1
bind = $mainMod SHIFT, 2, split-movetoworkspace, 2
bind = $mainMod SHIFT, 3, split-movetoworkspace, 3
bind = $mainMod SHIFT, 4, split-movetoworkspace, 4
bind = $mainMod SHIFT, 5, split-movetoworkspace, 5
bind = $mainMod SHIFT, 6, split-movetoworkspace, 6
bind = $mainMod SHIFT, 7, split-movetoworkspace, 7
bind = $mainMod SHIFT, 8, split-movetoworkspace, 8
bind = $mainMod SHIFT, 9, split-movetoworkspace, 9
bind = $mainMod SHIFT, 0, split-movetoworkspace, 10

# Scroll through workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Special Workspace (Magic)
bind = $mainMod, W, togglespecialworkspace, magic
bind = $mainMod SHIFT, W, movetoworkspace, special:magic

# ==========================================
# 5. MULTI-MONITOR CONTROLS
# ==========================================
bind = $mainMod, comma, focusmonitor, l
bind = $mainMod, period, focusmonitor, r
bind = $mainMod SHIFT, B, exec, sh ~/.config/hypr/scripts/rofi_wallpaper_selector.sh

# ==========================================
# 6. PYPRLAND SCRATCHPADS & PLUGINS
# ==========================================
# TUI / Dropdowns
bind = $mainMod, T, exec, pypr toggle term           # Dropdown terminal
bind = $mainMod, U, exec, pypr toggle volume         # Slide-in volume mixer
bind = $mainMod, I, exec, pypr toggle wifi           # Slide-in Wi-Fi manager

# Expose & Rofi Menus
bind = $mainMod, Tab, exec, pypr expose              # Mission control overview
bind = $mainMod, A, exec, pypr fetch_client_menu     # Rofi window switcher

# Layout Center
bind = $mainMod, Z, exec, pypr layout_center toggle  # Centers active window
bind = $mainMod, bracketleft, exec, pypr layout_center prev  # Navigate center layout left ([)
bind = $mainMod, bracketright, exec, pypr layout_center next # Navigate center layout right (])

# Pyprland Utilities
bind = $mainMod, N, exec, pypr shift_monitors +1       # Shift workspaces to next monitor
bind = $mainMod SHIFT, N, exec, pypr shift_monitors -1 # Shift workspaces to prev monitor
bind = $mainMod, Backspace, exec, pypr lost_windows    # Rescue off-screen windows

# Keyboard RGB Selector
bind = $mainMod, K, exec, sh ~/.config/hypr/scripts/rgb_keyboard_selector.sh

# ==========================================
# 7. SCREENSHOTS (Hyprshot + Swappy)
# ==========================================
bind = , Print, exec, sh -c 'hyprshot -m output --raw | swappy -f -'              # Full Screen
bind = $mainMod, Print, exec, sh -c 'hyprshot -m window --raw | swappy -f -'      # Active Window
bind = $mainMod SHIFT, S, exec, sh -c 'hyprshot -m region --raw | swappy -f -'    # Select Region

# ==========================================
# 8. HARDWARE & MEDIA CONTROLS
# ==========================================
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
