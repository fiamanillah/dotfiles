# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

### MY PROGRAMS ###
###################
# DEFINE THESE FIRST!
$terminal = kitty
$fileManager = nautilus
$menu = rofi -show drun -show-icons

# THEN SOURCE YOUR FILES
source = ~/.config/hypr/keybinds.conf
source = ~/.config/hypr/looks.conf
source = ~/.config/hypr/rules.conf # Renamed from window&workspaces.conf for safer parsing
source = ~/.config/hypr/monitors.conf
# source = ~/.config/hypr/workspaces.conf


#################
### AUTOSTART ###
#################
# Autostart necessary processes (like notifications daemons, status bars, etc.)
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # Essential for screen sharing
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = pypr
exec-once = blueman-applet
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = hypridle
exec-once = hyprpm reload -n
exec-once = hyprpanel
exec-once = swww-daemon
exec-once = ~/.config/hypr/scripts/swww-changer.sh


#############################
### ENVIRONMENT VARIABLES ###
#############################
env = XCURSOR_SIZE,20
env = HYPRCURSOR_SIZE,20
env = HYPRSHOT_DIR,$HOME/Pictures/Screenshots

# Fix for blurry XWayland apps when using fractional scaling
xwayland {
    force_zero_scaling = true
}

# ==========================================
# HARDWARE SPECIFIC SOURCING
# Uncomment the correct file for the current machine
# ==========================================
source = ~/.config/hypr/env_nvidia.conf  # Use this on the laptop
# source = ~/.config/hypr/env_intel.conf   # Use this on the office PC


#############
### INPUT ###
#############
# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 2
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
################
### GESTURES ###
################
# Adjust the feel of the workspace swipe
gestures {
    workspace_swipe_distance = 300 # How far you have to swipe to trigger it
    workspace_swipe_invert = true  # Set to false if swiping left/right feels backwards
}

# Define your specific 3-finger actions (Root level)
gesture = 3, horizontal, workspace
gesture = 3, down, mod: ALT, close
gesture = 3, up, mod: SUPER, scale: 1.5, fullscreen

# Example per-device config
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}



# ==========================================
# PLUGIN CONFIGURATION
# ==========================================
plugin {
    split-monitor-workspaces {
        count = 10
        keep_focused = 0
        enable_notifications = 0
        enable_persistent_workspaces = 0
    }
}